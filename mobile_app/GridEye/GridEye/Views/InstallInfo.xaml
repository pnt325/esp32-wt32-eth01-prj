<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GridEye.Views.InstallInfo"
             x:Name="this">
  <ContentView.Content>
        <StackLayout>
            <!--step index-->
            <Label x:Name="txtStep" 
                   Text="{Binding StepText}" 
                   FontSize="40"
                   HorizontalOptions="Center"
                   FontAttributes="Bold"/>
            <!--step information-->
            <Label x:Name="txtInfo" 
                   Text="{Binding StepInfo}"
                   FontSize="20"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"/>
        </StackLayout>
  </ContentView.Content>
</ContentView>